# StudyOS 总结

一个把「考研 408 + 数学 + 英语 + 项目 + 算法 + 六级」放进同一张每日计划的单文件网页。
你只需要改约束（截止日、时间、优先级），它就自动重算出"每天学多久、学哪几类"。

**位置**：`D:\codex\StudyOS\`（全部在 D 盘，C 盘不放东西）

---

## 1. 现在你拥有的东西

| 文件 | 作用 |
| --- | --- |
| `outputs\study_os.html` | 网页本体。**只有这一个文件是你要用的**，约 45KB，双击即用，不需要装任何东西 |
| `outputs\manifest.json` | 手机"添加到主屏幕"用的应用信息（名字、图标、全屏显示） |
| `outputs\icon.svg` | 主屏幕图标 |
| `启动手机访问.bat` | 双击后手机就能在同一个 Wi-Fi 下打开它 |
| `phone-help.txt` | 上面那个 bat 打印出来的中文说明 |
| `work\check.js` | 排程算法验证脚本（120 天全量校验） |
| `work\check_ui.js` | 界面行为验证脚本（25 项断言） |
| `work\dump.js` | 把页面渲染结果打印成文本，用来快速看排版 |
| `README.md` | 简版说明 |
| `StudyOS-总结.md` | 本文档 |

---

## 2. 怎么打开

**电脑**：双击 `outputs\study_os.html`，或浏览器输入

```
file:///D:/codex/StudyOS/outputs/study_os.html
```

**手机**：双击 `启动手机访问.bat` → 黑窗口会列出地址，手机连同一个 Wi-Fi 后浏览器输入，例如

```
http://192.168.43.127:8000/outputs/study_os.html
```

几点注意：

- 电脑上那个黑窗口**不能关**，关掉手机就连不上；电脑关机和休眠也一样。
- 第一次运行 Windows 会问"是否允许 Python 访问网络"，选**允许**并勾上**专用网络**，否则手机连不进来。
- 每次开机后电脑的 IP 可能变，以 bat 窗口里打印的为准。
- 打开后点浏览器菜单的"添加到主屏幕"，以后就是独立图标，全屏无地址栏。
- 我改过文件之后，手机刷新一下就能看到新版，不用重新添加。

---

## 3. 每天怎么用

1. 打开页面，看中间"未来 7 天"里今天那一列：固定课是什么、每一类学多久。
2. 右边"今日计划"按方向分组列出今天要碰的任务，做完就勾掉。
3. 情况变了（某门课作业多、项目延期、今天累了、多出一个 deadline）就在**学习设置**或**任务库**里改，然后点右上角"✨ 智能重排"，后面所有安排会自己跟着调整。

排程的核心承诺是：**你只改约束，剩下的重算交给它**，并且永远不占用固定课时间。

---

## 4. 界面上有什么

**总览**

- 四个统计：今日计划 / 可用多少、任务完成率、待完成任务数、最近截止日
- 未来 7 天：每列一天，显示固定课卡片 + 按方向的自习卡片（彩色左边框：408 红、数学 蓝、英语 绿、项目 紫、算法 黄）
- 今日计划：按方向分组，每组标这一类的总时长，逐条任务带勾选框
- 装不下时的提示：会告诉你哪个任务还差几小时、截止哪天
- 近期关键任务 + 学习闭环说明

**任务库**：13 个预置任务（数据结构复盘、组成原理复盘、OS/计网课程同步、两门课的考试冲刺、数学一基础、英语一词汇阅读、Java 基础、Spring Boot 项目 V1、Redis/Linux/Git、算法基础、六级刷分）。可以新增、编辑、删除、按状态筛选；删除后会立刻重排，8 秒内可以撤销。

**学习设置**

- 每天机动时间（从可用时间里预留，默认 0.5h）
- 最小学习单元（低于这个时长不单独安排，默认 25 分钟）
- 单个任务每天上限（默认 2.5h）
- 学期最后一天（默认 2026-11-02，之后课表自动失效）
- 可用时间窗（日常 / 学期结束后两套，纯文本格式，例如 `一 19:00-22:00`）
- 固定课表（纯文本格式，例如 `一 10:05-11:50 操作系统 @N333`）
- 导出 / 导入 JSON 备份、恢复默认模板

---

## 5. 排程到底是怎么算的

这是它和普通 Todo List 的区别所在。

1. **先算这天有多少小时能用**：可用时间窗（你的作息里真正能坐下来的时段）减去当天固定课表，再减去机动时间，得到当天的计划容量。所以你周一满课只剩 19:00–22:00，容量 2.5h；周六能到 9.3h。
2. **每个任务每天累积"应做量"**：`剩余工作量 ÷ 到截止日还剩几个能学的天`。所以远期任务不会全被挤到最后几天，而是每天推进一点。
3. **攒够一个最小学习单元才落进计划**：不够 25 分钟就继续攒，避免出现"408 今天 8 分钟"这种碎片。
4. **同一天内按分数抢容量**：`优先级×12 + 紧迫度 + 方向权重`。紧迫度按离截止的天数递增，逾期直接顶格；方向权重是 408:20、项目:14、数学:12、英语:8、算法:6、六级:2。
5. **同一任务同一天合并成一条**，不会出现重复条目。
6. **时间窗只用来算容量，不规定几点学**：哪天学、几点学、先做哪门，完全你自己定。

排完 120 天的结果：总任务 518 小时，120 天可用 923 小时，全部排完，平均每天 4.3h。留白是有意的——计划压着截止日走，不追求把可用时间填满。

---

## 6. 当前参数与预置数据

| 项目 | 值 |
| --- | --- |
| 排程视野 | 120 天 |
| 学期最后一天 | 2026-11-02 |
| 每天机动时间 | 0.5 小时 |
| 最小学习单元 | 25 分钟 |
| 单个任务每天上限 | 2.5 小时 |

可用时间窗（默认）：

| 星期 | 时段 | 说明 |
| --- | --- | --- |
| 周一 | 19:00–22:00 | 早八 + 四门课，白天没有连续空档 |
| 周二 | 09:00–12:15、14:30–16:00、19:00–22:00 | 只有 16:05 一门 |
| 周三 | 19:00–22:00 | 早八 + 三门课 |
| 周四 | 09:00–12:15、14:30–16:00、19:00–22:00 | 只有 16:05 一门 |
| 周五 | 09:00–10:00、15:45–18:00、19:00–22:00 | 两门课 |
| 周六 | 09:00–12:15、14:30–18:00、19:00–22:00 | 无课 |
| 周日 | 09:00–12:15、19:00–22:00 | 下午市场营销 |

午休 12:30–14:30、洗澡饭后、23:00 之后都不排。

---

## 7. 数据存在哪、怎么同步

- 任务、完成状态、所有设置都存在**浏览器本地**（localStorage，键名 `study_os_v3`），服务器上什么都没有。
- 打开旧版（`study_os_v2`）的数据时会自动尝试迁移过来。
- 所以**电脑和手机是两套独立数据**：手机上勾的任务，电脑看不到，反之亦然。
- 手动同步：电脑「学习设置 → 导出备份」下载 JSON → 传到手机 → 「导入备份」。
- 自动同步需要接一个后端或免费云数据库（Supabase / Firebase），目前没做。

---

## 8. 版本管理

项目已经在 git 里（仓库根目录就是 `D:\codex\StudyOS`）。任何时候都能退回旧版：

```
cd D:\codex\StudyOS
git log --oneline                    # 看历史
git diff                             # 看还没提交的改动
git restore outputs/study_os.html    # 丢掉未提交的改动
git checkout <提交号> -- outputs/study_os.html   # 取回某个历史版本
```

当前提交历史：

| 提交 | 内容 |
| --- | --- |
| `399d5a7` | 手机访问：启动脚本 + manifest + 图标 + README |
| `3b26d74` | 恢复旧版日历排版，补回丢失的 class-head/class-chip 样式，按方向配色 |
| `ae00827` | 日历去掉任务级分钟标注与钟点 |
| `e0a6220` | 从 C 盘迁移到 D 盘，补 README |
| `9833800` | 首次纳入版本管理 |

---

## 9. 怎么确认它还是好的

改完代码后跑这两条，两条都应该是 `问题数: 0` 和 `失败数: 0`：

```
cd D:\codex\StudyOS
node work\check.js outputs\study_os.html
node work\check_ui.js outputs\study_os.html
```

`check.js` 校验排程不会超出当天容量、不超单任务日上限、不重复、不排到最早开始日之前或截止日之后、完成任务不再被排。
`check_ui.js` 校验渲染、增删改、撤销、勾选完成、设置解析往返、非法输入报错、旧数据迁移等 25 项。

---

## 10. 现在的限制

- 手机访问依赖电脑开着 + 黑窗口开着；出门在外用不了（除非发布到网上）。
- 手机和电脑数据不互通，没有自动同步。
- 完成状态是"做了 / 没做"，还不能记录"今天实际只做了 1 小时"，所以暂时做不到"少学了自动补回来"。
- 没有阶段/目标层，任务是平铺的一层（还没做 2026.09–10 强化、2026.11 冲刺这种阶段划分）。
- 课表和时间窗要在设置页用文本格式改，没有可视化编辑器。
- 排程偏保守，压着截止日走，所以平均每天 4.3h 而不是把 9 小时填满。

---

## 11. 下一步可以做的

按优先级排：

1. **记录实际用时 + 自动补进度**：支持"今天 408 只做了 1 小时"，系统把差量摊到后面几天，低优先级后移，硬截止不受影响。
2. **阶段/目标层**：给任务挂到"2026.09–10 课程+408 基础"、"2026.11 强化"、"2027 春 408 主线"这类阶段上，能看到阶段进度。
3. **提前完成天数**：现在按截止日压线走，如果希望每件事都提前 20 天完成，加一个旋钮即可。
4. **发布到网上**：GitHub Pages / Vercel，随时随地打开，还能当 App 装到手机上，彻底摆脱"电脑必须开着"。
5. **跨设备同步**：接免费云数据库，电脑手机共用一份数据。
6. **课表可视化编辑**：不用再手写文本格式。
